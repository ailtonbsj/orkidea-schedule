{
  "name": "orkidea-schedule",
  "version": "0.0.1",
  "description": "A responsive plugin to show time intervals in a schedule of activities.",
  "main": "scheduler.js",
  "scripts": {
  	"dist-js" : "./node_modules/.bin/node-minify --compressor all --input 'schedule.js' --output 'schedule.min.js'",
  	"dist-css" : "./node_modules/.bin/node-minify --compressor clean-css --input 'schedule.css' --output 'schedule.min.css'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "http://github.com/ailtonbsj/"
  },
  "keywords": [
    "schedule",
    "viewer",
    "js"
  ],
  "author": "ailtonbsj",
  "license": "ISC",
  "devDependencies": {
    "node-minify": "^3.1.0"
  }
}
